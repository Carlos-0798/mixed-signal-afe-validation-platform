# Test data

No bench data exists. Generated files must include `source=SYNTHETIC`; future measurements must include the hardware, wiring, instrument, conditions, timestamp, and raw values.

`golden/crc16_ccitt_false.json` contains fixed algorithm test vectors, not measurements. These vectors verify software compatibility only and carry no BENCH evidence claim.
