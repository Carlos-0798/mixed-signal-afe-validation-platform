# 技术债与已知缺口

**更新日期：** 2026-09-09<br>
**来源：** Software Phase 0–6 与 post-beta 产品增量持续审计

优先级：`P0` 阻塞安全或正确性；`P1` 阻塞下一主要里程碑；`P2` 应在 v1 前解决；`P3` 可后置。

**当前执行顺序（2026-09-09）：** TD-050/TD-051/TD-052 已完成；暂停功能扩展，
先留档并同步私有 GitHub 开发分支，随后集中准备求职材料。恢复入口见
[项目接续点](PROJECT_RESUME_CHECKPOINT_2026-09-09.md)。TD-053 尚未启动，按真实样本重新选择范围。
TD-049 保留为历史冻结基线。
所有者重新授权独立审视核心运行层和用户界面，本轮围绕可复用配置、完整报告、
及时取消与批次失败保留成果收敛，见 TD-051 和 `docs/PRODUCT_REVIEW_2026-09-09.md`。
之后所有者批准 TD-052：先以通用电压文件导入扩大适用范围，再评估通用串口文本。
其余能力保持延期，不因宽泛授权自动启动框架迁移、云服务或硬件接入。
TD-043B2A 仍是屏幕阅读器支持的 P1 缺口，但运行时迁移按所有者决定继续暂缓，
不阻塞当前离线任务基线。P2 不代表必须为本次展示补齐所有功能；
v1 范围与公开发布另行决定。已关闭事项保留，不重复实施。

| ID | 优先级 | 技术债/缺口 | 影响 | 计划处理 |
|---|---|---|---|---|
| TD-001 | CLOSED | Git 身份和首个 Phase 0 基线于 2026-08-29 建立 | 已具有可恢复基线和历史差异 | 后续阶段保持小步提交 |
| TD-002 | CLOSED | 正式核心已迁入 `src/analog_validation/`，旧 protocol/shared-model files 与根 `dashboard/measurements` 均已删除；产品层位于独立 `src/analog_validation_app/` | UI/产品、领域和协议依赖方向已分离，正式算法只有一份 | 正式迁移见 Phase 1/3 reports；最终占位清理见 Phase 5 Step 1 report |
| TD-003 | CLOSED | 正式 Measurement 强制来源、状态、质量、单位、UTC 时间和原始引用 | 合成、仿真、回放与 BENCH 标签不再依赖文件名 | 证据见 `reports/software-phase1-step3.md` |
| TD-004 | CLOSED | Measurement、CRC、framing、AFE v1 profile 和 validation config 已版本化；20 条合法与 9 类非法 AFE 黄金消息已冻结 | 字段、CRC、模型意义和错误家族的意外漂移可由 pytest 发现 | 证据见 `reports/software-phase1-step8.md` |
| TD-005 | CLOSED | `DeviceCapabilities` 已要求显式通道、安全范围、命令和 safe-shutdown 一致性 | 软件领域层不再需要根据板名猜测功能；线上协商仍属后续实现 | 证据见 `reports/software-phase1-step4.md` |
| TD-006 | CLOSED | Phase 4 Step 1 已实现 profile-neutral 有界 LF 字节流、超长恢复和 profile-configurable modular sequence tracking | 分段、粘包、超长、16/32-bit 回绕、缺帧、重复和乱序已有 HOST_TEST；真实 serial lifecycle 由 TD-025 跟踪 | 证据见 `reports/software-phase4-step1.md` |
| TD-007 | CLOSED | 正式 DC 与迟滞算法位于 `analog_validation.analysis`，具备有限值/单位/来源/质量/方向/可追溯约束；旧 dashboard 函数与 legacy-only tests 已删除 | 正式产品分析不再依赖裸 tuple 入口，也没有第二份较弱公式 | 证据见 Phase 3 Step 2/5 和 Phase 5 Step 1 reports |
| TD-008 | CLOSED | `dc-sweep-analysis.v1` 保留所有点、输入/输出双引用、组件质量决定、逐点 `LOW/HIGH_SATURATION` 和 DC 排除原因 | 正式 DC 结果可解释具体使用或排除的每个点；旧函数不属于正式核心 | 证据见 `docs/dc-sweep-analysis.md` 和 `reports/software-phase3-step2.md` |
| TD-009 | CLOSED | `result-export.v1` 已覆盖 DC/迟滞/校准/频响 typed builders、稳定 JSON/CSV 和安全写入；`human-report.v1` 已为四类分析提供 text/Markdown/HTML/SVG/manifest，频响图只复制 finalized 幅值与截止指标 | 四类产品分析均有可追溯叙述和确定性图表；报告层不再调用底层拟合或截止插值 | 证据见 `docs/result-exports.md`、`docs/human-reports.md`、校准/频响产品报告；物理证据仍由 TD-018 跟踪 |
| TD-010 | CLOSED | 正式 `src/analog_validation/` 包、单一版本来源、隔离构建和仓库外 wheel 导入已于 2026-08-29 验证 | 包装基础问题已解除；CLI 仍由 TD-014 跟踪 | 证据见 `reports/software-phase1-step1.md` |
| TD-011 | CLOSED | 独立 Python `.venv`、setuptools 和隔离 wheel 安装已于 2026-08-29 验证 | 原环境依赖 Codex 运行时的问题已解除 | 证据见 `reports/environment-setup-2026-08-29.md` |
| TD-012 | CLOSED | 冻结的 100 帧生成器已迁入正式 package，并由 CLI、集成测试和确定性 SimulatorAdapter 共同复用；stream SHA-256 保持不变；非理想和受控故障已在 Step 4 配置化 | 合成基础流水线不再存在第二份公式，增益/噪声/饱和/迟滞/故障均有确定性回归 | 证据见 `reports/software-phase2-step3.md` 和 `reports/software-phase2-step4.md` |
| TD-013 | CLOSED | 只读 GitHub Actions 保留 Windows/Ubuntu 与 Python 3.10/3.12/3.14 矩阵及 quality/release verifier；2026-09-09 按所有者决定仅手动触发 | 本地测试为默认；push/PR 不自动使用云端额度，手动运行仍保留完整门禁；workflow 无 write permission 或发布动作 | 历史证据见 `reports/software-phase6-step2.md`、`reports/software-phase6-step4.md` 和 run `33447031789`；现行策略见 `docs/LOCAL_TESTING_AND_CI.md` |
| TD-014 | CLOSED | 单一 `analog-validation` 已安装并提供稳定 human/JSON 的 version/profiles/ports、Simulator/Replay read/live monitor/DC/迟滞/校准/频响、系数检查、显式 receive-only observe、确定性 `report`、六步本地 `dashboard` 和 12-artifact `demo` | 用户可运行正式软件验证、报告、reviewed Dashboard workflow、有界实时曲线、versioned coefficient persistence、幅值频响验收和完整 synthetic demo；所有入口共享请求编译与 worker service，不维护第二套工程逻辑 | 证据见 Phase 5 Steps 3–7 reports、`docs/product-cli.md`、`docs/dashboard.md`、`docs/live-monitoring.md`、校准/频响产品报告和 `docs/software-demo.md` |
| TD-015 | CLOSED | CRC vectors、AFE wire records、预期 model JSON 和非法输入错误家族均已冻结 | 文件兼容性已有 host regression 保护 | 未来 schema 变化必须添加迁移样本 |
| TD-016 | CLOSED | 正式包已建立 Validation、Protocol、Framing、CRC、版本、Capability、Configuration 和 Adapter 错误层级；旧协议入口已删除 | UI/CLI 已有稳定捕获边界 | 证据见 `reports/software-phase1-step2.md`、Step 8 closure 和 `reports/software-phase2-step1.md` |
| TD-017 | P3 | 采购和控制器选择文档仍包含软件转向前候选 | 可能误读为立即采购指令 | 保留历史；采购前由新规划重新冻结 |
| TD-018 | P0 | 所有 AFE 硬件性能仍未验证；Step 7 只有 controller UART compatibility BENCH 证据 | 把窄范围串口证据扩大为模拟性能声明会损害可信度和安全 | 持续保持 AFE `VERIFIED_BENCH=0`，直到真实模拟台架阶段，并把两类证据分栏 |
| TD-019 | CLOSED | `validation-config.v1` 已使用不可执行的严格 JSON，限制文件大小，并对 profile、通道、单位、超时、来源和输出边界执行分层验证；DeviceAdapter 在 I/O 前复用这些安全门 | 配置不依赖任意 Python 代码；具体 Simulator/CSV/Serial I/O 仍按阶段实现 | 证据见 `reports/software-phase1-step7.md` 和 `reports/software-phase2-step1.md` |
| TD-020 | CLOSED | `csv-replay.v1` 已冻结 13 列、META/DATA/END、UTC、单位、状态、声明来源、质量、记录引用和限制；loader 只读且合法/非法黄金样本冻结错误族；Step 6 Dashboard 在 Run 前预检并绑定不可变 dataset | 历史数据不再依赖猜测列、单位或文件是否完整；缺失/非法输入不会先启动 worker | 证据见 `docs/csv-replay-v1.md`、`docs/dashboard.md` 和 Phase 2 Step 5/Phase 5 Step 6 reports |
| TD-021 | CLOSED | 正式 `CsvReplayAdapter` 已提供显式只读能力、独立通道游标、立即/缩放时间、运行时速度、暂停/恢复、明确 EOF、引用保留和强制 `CSV_REPLAY` 来源，并通过共用 adapter 契约 | 历史文件可通过正式设备端口消费，不会把文件声明的 `BENCH_*` 提升为当前实物证据 | 证据见 `docs/adapters.md` 和 `reports/software-phase2-step6.md` |
| TD-022 | CLOSED | `read-workflow.v1` 已用不可变请求/结果和同一函数驱动 Simulator/CSV；全量能力预检发生在读取前，明确区分 `COMPLETED`、`UNSUPPORTED` 与 `INCOMPLETE`，所有路径释放 workflow 自己拥有的 adapter | 上层采集不再根据来源写分支，也不会把缺能力、数据耗尽或执行错误混成一种状态 | 证据见 `docs/read-workflow.md` 和 `reports/software-phase2-step7.md` |
| TD-023 | CLOSED | `phase2_public_api.json` 冻结公开 imports/schema/enum/signature/error/replay hash，`phase2_workflow_v1.json` 冻结 Simulator/CSV/UNSUPPORTED 端到端含义；隔离构建和仓库外 wheel 验证纳入阶段出口 | Phase 2 兼容性变化不再能静默发生；未来破坏性变更必须升级版本并记录迁移 | 证据见 `docs/phase2-public-api.md` 和 `reports/software-phase2-step8.md` |
| TD-024 | CLOSED | `phase3_public_api.json` 冻结公开 imports/schema/enum/signature/error/constants 与三份结果文件哈希；exact `SYNTHETIC` DC/迟滞结果冻结拟合、饱和排除、阈值、迟滞宽度及来源语义 | Phase 3 调用和结果兼容性变化不再能静默发生；这些基准不构成实物性能声明 | 证据见 `docs/phase3-public-api.md` 和 `reports/software-phase3-step8.md` |
| TD-025 | P2 | Phase 4 Step 7 已完成独立可选 `analog_validation_pyserial` backend、base/serial extras 外部安装、COM 枚举和一次 receive-only MSP430 SerialAdapter→ReadWorkflow HIL；Phase 5 Steps 3/5/6 已分别验证 CLI 中断、GUI close、六步 Run/Cancel/cleanup 和内存串口零 write，但交互式 Windows console event、long-duration timing、physical reconnect 和真实 COM worker lifecycle 仍未测试 | 产品已有安全的 owner/cancel/cleanup、CLI 中断和 Dashboard 运行边界，但当前物理证据仍仅为先前 COM4 五帧、单次打开/关闭、零写入；不能推广为生产级串口可靠性 | console/long-duration/disconnect/real-COM worker 仍需单独评审和授权；见 `docs/product-worker.md`、`docs/dashboard.md`、Phase 5 Steps 2–6 和 Phase 4 Step 7 reports |
| TD-026 | CLOSED | `protocol.envelope` 已提供不要求 namespace 的严格 token/CRC record；`framing.py` 仅保留 AFE shape wrapper | AFE 与 MSP430 形状可复用同一 envelope，旧 AFE API/bytes/errors 不变 | 证据见 `reports/software-phase4-step2.md`、三条 neutral golden records 和旧 20 valid/9 invalid 回归 |
| TD-027 | CLOSED | `afe-channel-map.v1` 已冻结 canonical `input/output/gain/threshold` 与 legacy telemetry v1 `input_mv/output_mv/gain/threshold`，转换必须显式声明 source/target；Step 4 AFE serial profile 已实际跨越此边界 | 串口 telemetry 已对齐 Simulator/workflow，同时历史 mapper/Measurement 不被静默重命名 | 证据见 `docs/afe-channel-mapping.md`、`docs/serial-profiles.md` 和对应 tests |
| TD-028 | CLOSED | 独立 MSP430 Equipment Health v1 parser/profile 和本仓库 10 valid/11 invalid fixtures 已实现；支持 device-output `TEL/ACK/STS/CFG/LOG`、32-bit TEL continuity、sentinel/fault mapping、typed raw outcome 和静态只读 capabilities | 不再依赖人工字段解析；Step 6 已完成 receive-only adapter，Step 7 已完成窄范围 OS/HIL，剩余 long-duration/physical reconnect 由 TD-025 跟踪 | 证据见 `docs/serial-profiles.md`、`reports/software-phase4-step5.md`、`reports/software-phase4-step6.md` 和 `reports/software-phase4-step7.md` |
| TD-029 | CLOSED | `serial-profile.v1` 和独立 `AfeV1SerialProfile` 已实现显式 identity、16-bit TEL continuity、严格 capability aggregation、canonical telemetry mapping、typed raw outcome 和失败回滚；20 valid/9 invalid golden 均通过新路径 | AFE 业务字段不再由调用者临时拼接解析，且 transport/profile/高层依赖方向已有执行门禁 | 证据见 `docs/serial-profiles.md` 和 `reports/software-phase4-step4.md` |
| TD-030 | CLOSED | Step 6 已建立显式 AFE capability projector：`adcN/dacN/pwmN/dinN` 映射为 `afe.chN.input/dac/pwm/threshold`，保留 native snapshot，并验证 identity、channel counts、numeric ranges 与 command subset | workflow channel 已与 telemetry 对齐；projector 不能增加命令或把 receive-only adapter 提升为输出设备 | 证据见 `src/analog_validation/serial_adapters/afe_v1.py`、共用/投影防御测试和 `reports/software-phase4-step6.md` |
| TD-031 | P2 | Measurement v1 尚无 watt/milliwatt 单位；MSP430 `power_mw` 已保留在 typed telemetry 和 INA219 availability 语义中，但未伪装为 `UNITLESS` Measurement | 通用 workflow 暂时不能按正式 Measurement channel 直接读取功率；错误添加单位会漂移 Phase 2/3 冻结契约 | 在后续 schema/version 评审中增加 power unit 与迁移样本；在此之前由 typed raw message 审计，不静默换单位 |
| TD-032 | CLOSED | `phase4_public_api.json` 已冻结 121 exports、3 schemas、12 enum/flag sets、21 signatures、17 error relationships 和 5 hashes；`phase4_composite_v1.json` 已冻结 AFE/MSP external-backend exact results | Phase 4 transport/profile/adapter 兼容性变化不再能静默发生；第三方 backend 无需继承内部类，但必须满足结构协议 | 证据见 `docs/phase4-public-api.md`、13 项 golden tests 和 `reports/software-phase4-step8.md` |
| TD-033 | CLOSED | Phase 5 Step 1 已建立 `analog_validation_app`，删除根 `dashboard/` Python 占位与两个 legacy-only analysis tests；Steps 2–8 worker/factories/services/shared workflow/CLI/presentation/reporting/Dashboard/demo/freeze 继续通过单向架构门禁 | 六步 UI、demo 和 compatibility freeze 均通过正式 product services 接线，没有重新引入旧占位或复制工程算法 | 2,189 项完整回归、11,470/11,470 package statements 与 Phase 1–4 goldens 继续通过；见 Phase 5 Steps 1–8 reports |
| TD-034 | P3 | Ruff 0.16.5 的规则检查已通过，但全仓库 formatter 仍建议重排历史 Python 文件；Step 6 的 20 个新增/修改 Python 文件自身已通过格式检查 | 不影响当前行为或 lint 正确性，但未来全仓统一格式会产生较大、低信号 diff | 在独立 formatting-only 变更中处理，先冻结工具版本并重新跑完整测试；不得混入功能提交 |
| TD-035 | P3 | Phase 5 Step 8 正式门禁保持 11,470/11,470 语句覆盖；Step 7 的额外全仓分支 instrumentation 计算为 99.90%、15 条 partial branches，但其额外开销使 5 秒 Replay 性能检查返回 false，因而当时的诊断运行是 1 failed/2,174 passed，不是通过的正式门禁 | 当前未执行 package 语句仍为 0，独立无 instrumentation 的产品质量工具实测 10k Replay 为 0.823268 s 并通过；但把性能验收与覆盖 instrumentation 混跑会产生误导性失败 | 文档始终写明 statement coverage；Phase 6 将 timing acceptance 与 branch diagnostic 分离，并评审 15 条分支是不可达、防御性还是需要新增测试 |
| TD-036 | CLOSED | 旧深层 `work/` venv 曾触发 `WinError 206`，而受管 Windows 又以 `WinError 4551` 阻止系统临时目录中的 pip console launcher；verifier 保留系统临时构建，但把 fresh-install venv 放到调用者明确选择的候选父目录，使用短原子 staging 名、自动清理并保留真实 launcher 执行门禁 | 既避免深层仓库路径，又不要求关闭应用控制；用户若手动选择过深或受保护的输出路径仍可能失败 | 单元测试冻结显式现存父目录、同边界 cwd 与零残留；最终 base/serial-extra、normal/Unicode demo 和外部 adapter 链在短候选根下通过，故障排查文档保留两类 Windows 错误的安全恢复步骤 |
| TD-037 | CLOSED | `phase5_public_api.json` 已在既有冻结基础上评审校准/频响/live monitor/Serial device-contract/project-history 扩展，当前冻结 5 product namespaces、22 schemas、10 enums、49 dataclasses、60 signatures、29 error relationships、30 issue mappings、32 CLI paths、8 exits、22 serialized groups 和 6 golden hashes | Product/CLI/report/demo/project compatibility 变化不再能静默发生；breaking change 必须升级 contract/schema 并记录迁移 | 证据见 `docs/phase5-public-api.md`、对应 golden tests、`reports/software-phase5-step8.md` 和 post-beta 产品报告 |
| TD-038 | CLOSED | 原计划实时曲线已实现为有限 Simulator/CSV Replay/receive-only Serial `LIVE_MONITOR`：逐周期多通道读取、ring buffer、展示时间窗、质量/淘汰计数、协作暂停/恢复、CLI 和 Dashboard 均接入共享 worker；Serial 要求显式确认和 55 秒组合预算，AFE 双曲线另要求 exact ID 与完整 ADC map | 软件可在无学校设备时展示完整动态链路，同时不会因 UI 曲线暗示真实采样、硬实时、认证 identity、实物接线或可靠性 | 证据见 `docs/live-monitoring.md`、live monitor/device-contract tests 和对应阶段报告；真实设备/soak/断连仍由 TD-025 跟踪 |
| TD-039 | CLOSED | 原计划测试项目、预设、批量执行、历史和两次运行比较已实现为严格版本化本地文件：最多 32 个离线预设、显式顺序批次、create-new run、exact project snapshot、每预设配置 hash、artifact 验证和不重算结论的 comparison；项目拒绝 Serial 配置 | 多项既有工作流可被复用、追溯和比较，不再依赖复制长命令或让后续项目编辑抹去旧配置 | 证据见 `docs/test-projects-and-history.md`、67 项 focused tests、Phase 5 public golden 和 `reports/test-project-history-readiness-2026-09-06.md` |
| TD-040A | CLOSED | 核心批次与 CLI 已加入线程安全协作取消、逐预设真实进度、worker cleanup 后停止后续项、保留终态记录、显式 not-started 列表、`COMPLETE/PARTIAL/CANCELLED/ERROR` manifest v2、严格 v1 兼容、stderr/stdout 隔离和取消退出码 130；create-new、原子发布、拒绝覆盖及 artifact hash 校验保持不变 | 长批次现在能安全中止且部分历史可审计；状态表示执行终态，不把软件 PASS 或仿真升级为硬件证据 | 证据见 `docs/test-projects-and-history.md`、TD-040A focused/compatibility/golden tests 和 `reports/td-040a-core-cli-acceptance-2026-09-07.md` |
| TD-040B | CLOSED | Dashboard 已消费 TD-040A 的不可变进度事件和批次取消令牌；页面显示真实阶段/当前预设/计划与完成数，首次取消后禁用按钮，关闭时请求同一协作取消，并在 cleanup 与 manifest 原子发布完成后才退出；历史显示 v2 终态和 not-started 数，v1 明示 `LEGACY_V1` | CLI 与图形界面现共享同一核心批次语义，Tk 只在 owner thread 轮询队列；软件状态和 PASS 不提升证据等级 | 证据见 `docs/dashboard.md`、Dashboard workspace/widget/真实 Tk tests 和 `reports/td-040b-dashboard-batch-control-acceptance-2026-09-07.md` |
| TD-040C | P2 | 原始 READ/LIVE observation 持久化、曲线叠加和 manifest 签名/认证仍未实现；保留为汇总项 | 三类缺口对应不同任务，不应绑定一次实施 | 分别由 TD-040C1/C2/C3 跟踪；不因拆分标为 CLOSED，不修改历史 manifest |
| TD-040C1 | P2 | TD-040C1A 已完成离线 CSV Replay 输入归档，TD-040C1B 已将其只读显示到 Dashboard 历史；READ/LIVE 的输出 observation artifact 与从归档输入一键重跑仍未实现 | v3 历史可独立验证并让用户检查实际 Replay 输入，但 observation-only 工作流仍只有计数/摘要，复盘还需显式工具链 | 后续先由真实使用反馈判断一键重跑或 READ/LIVE 输出持久化的价值；不与曲线叠加混做 |
| TD-040C1A | CLOSED | `validation-run-manifest.v3` 为每个已执行 Replay 预设记录 create-new 输入副本的安全相对路径、字节数和 SHA-256；worker 从 staging 副本运行，同一源文件物理去重且保留逐预设引用，未开始项不打开；缺失、超限、复制失败及篡改均 fail closed；严格 v1/v2 原形读取 | 原 CSV 移动或修改后，已完成 run 仍保留可校验的实际输入字节；证据仍为 `CSV_REPLAY`，SHA-256 不提供作者认证 | 核心/CLI/golden/兼容/原子失败测试与全量质量门禁；见 `reports/td-040c1a-replay-input-archival-2026-09-08.md` |
| TD-040C1B | CLOSED | Dashboard 历史单选新增只读 Replay 输入表，显示逐预设归档路径、字节数和完整 SHA-256，并解释同源引用数/物理文件数、v1/v2、Simulator-only、未开始、多选比较及证据边界 | 核心可复现性能力现在对陌生用户可见；现有八列历史和双选比较手势保持不变，未增加重跑、文件打开或 schema | 25 项项目页面/真实 Tk 定向测试、五档 Windows 缩放、2,709 项完整回归和 16,356/16,356 statements；见 `reports/td-040c1b-dashboard-input-visibility-2026-09-08.md` |
| TD-040C2 | P3 | 比较页曲线叠加尚缺 | 已有数值比较，视觉定位复杂变化有限 | 仅在主任务记录显示数值/现有报告不足时推进 |
| TD-040C3 | P3 | manifest 作者认证、签名与信任模型尚缺 | 哈希只能检查相对完整性，不能认证作者 | 作为独立信任需求后置；不以本次效率对照为理由引入 |

| TD-041 | CLOSED | 已补充项目表格 Shift+方向键范围选择与 Ctrl+A 全选，并将真实 Tk 缩放测试隔离到新进程 | Windows 初始化、焦点、回调与超时错误均作为失败，不转为 skip；已完成真实桌面键盘验收 | 见 `reports/dashboard-keyboard-isolation-acceptance-2026-09-06.md`：统一回归 2,591 passed、无跳过；原始问题报告保留不变 |
| TD-042 | CLOSED | Dashboard 已统一为 Precision Lab Console 深色设计系统：石墨背景、分层卡片/输入、语义化主次/取消按钮、真实进度、30 px 表格行、当前步骤强调，以及始终可读的 LOCAL/OFFLINE、READ-ONLY DEFAULT、EVIDENCE LABELED 边界标签；项目表格不再把保存/目录/安全取消/清理按钮推出窗口 | Setup、Results、Projects 三页共享一套 ttk 样式，颜色不替代文字；无假数据、空闲动画、网络资源或额外 UI 定时器，既有回调和状态机不变 | unit widget tests 与 1.0/1.25/1.5/1.75/2.0 Windows Tk focus/geometry tests 冻结显示契约；见 `docs/dashboard.md` 和 `reports/dashboard-novice-product-evaluation-2026-09-07.md` |
| TD-043A | CLOSED | Dashboard 的表单解析与 workflow 约束现携带内部稳定字段键；失败后自动滚动、聚焦、用文字点名并以错误边框标出控件，修正/导航后清除；未知字段回退到原 issue 卡片 | 新手无需在密集配置页自行搜索第一个错误；`user-issue.v1`、CLI JSON、project/manifest 和 public API golden 均未改变 | fake-toolkit、应用协调器、跨字段约束及 Windows Tk 1.0/1.25/1.5/1.75/2.0 测试；见 `reports/td-043a-field-error-navigation-2026-09-07.md` |
| TD-043B1 | CLOSED | Dashboard 最小窗口降至 1040×760，密集配置改为三列分行，Results 固定宽度预算收紧；启动时只读 Windows 高对比度状态并用系统颜色覆盖 ttk、Canvas、焦点、选择、禁用与错误状态 | Setup/Results/Projects 在标准/强制高对比度及五档 Windows Tk 缩放下无关键控件横向越界；状态和错误仍有文字，不把颜色作为唯一语义 | 见 `reports/td-043b1-narrow-high-contrast-acceptance-2026-09-07.md`；低于 1040 px 不在当前支持范围，运行中切换主题需重启 |
| TD-043B2A | P1 | 当前 Windows 候选为 Tk 8.6.15，不提供 `tk accessible`；UI Automation 只看到 37 个无名称应用 Pane。能力门禁和 Tk 9.1 适配已就绪；TD-043B2A2 进一步排除 Tk 9.1 beta 生产路线，并用 PySide6 Essentials 原型证明六个应用控件可被 UIA 正确识别 | 当前 Dashboard 仍不支持屏幕阅读器；PySide6 候选增加约 200 MiB 环境体积和 LGPL/GPL/商业许可审查，不得只因原型成功便加入正式依赖 | 按 Proposed ADR-0004，在所有者批准依赖与许可证工作后实施 Simulator-only Qt 纵向切片；必须通过 UIA 事件、Narrator/NVDA、等价状态机、安装和真实用户链才能关闭；见两份 TD-043B2A 报告 |
| TD-043B2B | P2 | Remote Desktop、长期纯键盘真实用户操作、本地化、低于 1040 px 和运行中主题热切换尚无正式验收 | 现有自动化不能代表真实用户、所有显示环境或跨平台认证 | 分别在目标环境中验收；先冻结消息标识，再评估本地化与主题热切换 |
| TD-044A | CLOSED | Configure 页面加入按当前来源和六类测试生成的白话字段说明；操作按钮上方按真实向导权限显示下一步、阻塞原因及“尚未开始采集”边界；原先只藏在 Replay/Serial 区域的通用提示被移至 Simulator 也能看到的独立区域 | 新手无需根据禁用按钮猜测流程，也不会把 R-squared、RMSE、迟滞宽度、校准系数或频响幅值误解成未实现能力；显示文本直接消费既有不可变状态，不复制运行权限 | fake-toolkit、真实 Windows Tk 1040×760 标准/强制高对比度五档缩放、完整回归与 100% statement coverage；见 `reports/td-044a-contextual-novice-guidance-2026-09-08.md` |
| TD-044B | CLOSED | Source/Test 下拉框改用可读显示别名并严格往返到原稳定枚举；Replay 与 Serial 详情区域按当前来源互斥显示；Replay 加入原生 CSV 文件选择入口、明确 required/optional，并在取消时保留原路径 | 首次配置不再暴露内部枚举或无关来源字段，用户无需手输长路径；选择文件仍不读取内容，Review 继续承担唯一严格预检边界 | 94 项 Dashboard 定向测试、15/15 public API golden、真实 Windows Tk 1040×760 十组合、2,684 项完整回归与 100% statement coverage；见 `reports/td-044b-source-selection-replay-flow-2026-09-08.md` |
| TD-045A | CLOSED | Results 将执行终态、工程结论、证据含义、声明边界和下一步提升为独立决策摘要；详细区继续完整显示 summary、limitations、not-verified 和结构化 issue；无 outcome 的观察任务明确显示 `NO ENGINEERING DECISION` | 新手不再需要从一段等权文本中自行判断 `COMPLETED` 与 `PASS` 的差别，软件/回放/仿真 PASS 更难被误写为硬件验证；所有内容只解释现有不可变枚举和结果字段 | 85 项结果/工作流/真实 Tk 定向测试、九类证据文案矩阵、2,693 项完整回归、16,034/16,034 statement coverage；见 `reports/td-045a-result-decision-hierarchy-2026-09-08.md` |
| TD-045B | CLOSED | Projects & history 在既有 comparison 表上方加入只读解释层：明确同一项目、baseline/candidate、各自 batch 状态、project snapshot 是否变化、匹配/单侧/未开始结果、证据标签匹配与冲突、changed preset，以及 `candidate - baseline` 算术方向 | 新手能先判断证据和覆盖是否适合比较，再阅读数值；不同证据不会被暗示为 like-for-like，差值正负不会被自动解释为改善或退化；manifest v1/v2、comparison v1、PASS/FAIL 和公开 API 不变 | 22 项 workspace/widget/五档真实 Tk 定向测试、2,694 项完整回归、16,091/16,091 statement coverage、17/17 API/quality 与完整静态门禁；见 `reports/td-045b-project-history-comparison-guidance-2026-09-08.md` |
| TD-046A | CLOSED | Projects & history 加入由现有 workspace 权限派生的首次使用/下一步说明、当前 Setup 来源与测试预览、未保存与过期 Review 恢复提示、零/一/多条历史空状态；Save/Add/Review/Run/Compare/Clear 只在后台对应操作可能成功时启用 | 新手不再通过反复点击必然失败的按钮猜测流程；Serial Setup 明示不能存入项目且不发现端口，Simulator/Replay 保留准确证据标签；没有复制项目状态机或改变业务拒绝规则 | 23 项 workspace/widget/五档真实 Tk 定向测试、2,695 项完整回归、16,168/16,168 statement coverage、17/17 API/quality 与完整静态门禁；见 `reports/td-046a-project-first-use-guidance-2026-09-08.md` |
| TD-046B | CLOSED | Projects & history 在 Run 前将现有不可变 Review 投影为结构化摘要：project/run、create-new 目录、项目顺序中的 preset、可读 Source/Test 和逐项 `SYNTHETIC`/`CSV_REPLAY`；Run 消费 Review 后摘要清空 | 新手无需从选择框和路径输入反推“将运行什么、按什么顺序、写到哪里、证据是什么”；只读投影不复制执行状态机，不读取 Replay，不改变 manifest、结果、哈希、取消、CLI 或公共 API | 24 项 workspace/widget/五档真实 Tk 定向测试、2,696 项完整回归、16,210/16,210 statement coverage、17/17 API/quality 与完整静态门禁；见 `reports/td-046b-batch-review-summary-2026-09-08.md` |
| TD-048 | CLOSED | 重复 DC 任务的软件自动化价值缺少独立任务级证据 | 已用 5 个冻结案例证明 CSV Replay 入口、数据质量处理、拟合、工程判断、报告、项目历史和比较可组成一致的自动化链；不量化人工节省 | 258 项针对性检查和独立参考通过；人工基线由所有者决定延期并保持 NOT_RUN，未来只有需要时间/陌生用户效率主张时恢复；见任务方案与 TD-048 软件预演/关闭报告 |
| TD-049 | CLOSED | 完成当前未提交产品的功能冻结和成果收敛：冻结变更准则、延期清单和证据边界；从工作树外完整快照构建 sdist/wheel，fresh-install 后验证双路径确定性 demo、CSV Replay v3 历史自包含、15/15 产品质量与路径隐私 | 后续工作有明确进入条件，不再用低优先级功能扩大展示前风险；冻结本身不代表发布、硬件验证或 Git 基线 | 见 `docs/FEATURE_FREEZE.md` 和 `reports/feature-freeze-and-consolidation-2026-09-08.md`；下一步仅做 owner diff review/提交分组，外部动作仍需授权 |
| TD-050 | CLOSED | 所有者观察到旧配色文字辨识困难、强调色过多；新增 Workbench、Daylight、Midnight 三套克制配色和窗口内主题切换，正文/提示/表格至少 10 pt，文字/状态/图例配色对比度门禁 4.5:1；修正 disabled 状态优先级、弹出列表换色和项目滚动背景的高对比度遗漏 | 切换仅重绘显示层，保留未保存输入、错误、焦点、滚动、选中行、运行和结果；无新运行依赖、配置文件或数据契约；跨重启记忆主题暂不实现，选择器明确标注 this window | 见 `reports/td-050-dashboard-appearance-2026-09-09.md`；原 TD-042 单一主题被本轮三主题替代，TD-043B2A 屏幕阅读器限制仍存在 |
| TD-051 | CLOSED | 已存预设可复制载入 Setup 并重新 Review；GUI 可原子保存包含规范 JSON 的完整报告；普通采集逐次检查取消；manifest v4 明确后续准备失败，保留已完成成果，严格读取 v1–v3 | 核心自动化、用户操作和证据文件形成连续闭环；旧项目、结果和历史文件不原地迁移。无效的隐藏 Replay 范围不会再阻止切换后的 Simulator 任务 | 见 `docs/PRODUCT_REVIEW_2026-09-09.md` 和 `reports/product-runtime-and-workflow-review-2026-09-09.md`；逐次检查仍不能强制中断正在阻塞的 adapter 调用，READ/LIVE 全量持久化和原地预设编辑继续延期 |

| TD-052 | CLOSED | 普通电压表格此前必须人工转换成严格 Replay 格式；新增显式字段/单位/时间映射、模板、保留源文件的原子导入包及 CLI/Dashboard 闭环 | 将不同 CSV 布局接入既有分析、批次、历史和报告，避免每种布局重复写转换脚本；不宣称新增板卡兼容 | 见 `docs/voltage-data-import.md` 和 `reports/td-052-voltage-import-2026-09-09.md`；3,047 项完整测试、17,567/17,567 statements、真实 Tk 和独立安装后 23 条 CLI 命令通过 |
| TD-053 | P3 | 通用串口文本/JSON、更多物理量、多通道及设备特定二进制协议尚未实现 | 当前新入口覆盖可导出的电压表格，不能自动读取任意板卡；统一接口仍需协议适配与逐来源验收 | 先收集真实、可公开解释的数据样本，再选择下一种输入；复用本轮纯解析层，不自动扩大硬件访问或兼容声明 |

关闭技术债时必须记录对应代码、测试、文档和验证报告，不能只从表格删除。
