# 功能冻结与成果收敛

**冻结日期：** 2026-09-08<br>
**软件版本：** `0.1.0b1`<br>
**状态：** 2026-09-08 冻结基线保留；TD-050/TD-051/TD-052 已完成本地验收。
当前暂停功能扩展、准备求职展示，进入所有者授权的私有开发分支与既有 Draft PR 同步阶段。
尚未发布，不提前声明本次远程 CI 或合并通过。

## 冻结目的

Analog Validation Studio 已具备完整的软件主链：从受控数据输入、分析、工程判断、
批次执行、协作取消，到项目历史、可验证产物、报告、CLI 和 Dashboard。当前重点从
“继续增加功能”转为“保持稳定、便于安装、演示、解释和评审”。冻结不等于发布，
也不改变 Git 历史或任何外部可见状态。

## 已冻结的产品范围

- 控制器中立的测量、能力、安全范围、配置、协议和 adapter 契约；
- `SYNTHETIC` Simulator 与严格 `CSV_REPLAY` 的 read、DC、迟滞、校准、频响和有限
  live monitor 工作流；
- 单 owner worker、真实进度、暂停/恢复、批次级协作取消和 cleanup；
- `COMPLETE`、`PARTIAL`、`CANCELLED`、`ERROR` 批次语义，以及严格可读取的
  manifest v1/v2/v3；本轮新运行使用 v4，保留后续准备失败的先前成果；
- create-new、原子发布、不覆盖、SHA-256 完整性检查和 Replay 输入归档；
- 人类报告、结构化 JSON/CSV、确定性 12 产物演示、项目历史与比较；
- 本地 CLI 和 Precision Lab Console Dashboard 的新手引导与证据边界。

## 冻结后的变更准则

只有下列情况可以打破功能冻结，并且仍需独立评审和完整验证：

1. 会造成错误结论、数据损坏、安全边界失效或无法 cleanup 的 P0/P1 缺陷；
2. 已被真实重复任务或陌生用户观察到、会阻断主链的交互问题；
3. 已发布契约的兼容、安全、隐私或可安装性问题；
4. 发布前必须修复的文档与实际行为不一致。

其余想法进入技术债或后续路线，不在冻结期直接实现。每次例外都应说明触发证据、
影响范围、回归风险和验收结果，避免以“顺手优化”为由扩大变更。

### 2026-09-09：显示可读性例外

所有者实际使用后反馈文字辨识困难、配色不协调，并要求提供工作台、白天、深夜
等主题选择。TD-050 将此次例外限制在三套可读配色、窗口内切换、提示字号和系统
高对比度一致性；不扩展业务功能、不迁移 UI 运行时、不改变项目文件或任何数据契约。
回归重点为主题切换时保留输入、错误提示、焦点、滚动、选中行、当前任务和结果。
TD-050 验收时停在未提交状态；当前同步授权见下文。此前已验收的候选包和演练目录保持原样。

### 2026-09-09：所有者授权的产品整体审查

所有者进一步授权突破原规划、自主优化内部运行层与面向用户的交互层。
本轮选择四项可验证改进：预设载回配置页、图形界面完整报告包、逐次读取的协作取消、
后续预设准备失败时保留先前成果。旧冻结规则不再阻止这些已授权改进；
旧文件格式仍严格兼容，历史产物和此前提交保持不变。
范围与设计依据见 [产品重新审查](PRODUCT_REVIEW_2026-09-09.md)。

## 新增授权与后续边界

### 2026-09-09：所有者授权扩大文件接入范围

所有者希望在只有一块板卡的条件下扩大实际适用范围，并批准先推进通用文件导入。
TD-052 将本轮限制为显式电压列映射、可复用模板、保留源文件的导入包，以及 CLI 与
Dashboard 接入现有 Replay 分析。新格式单独版本化，不修改历史文件或扩大硬件声明。
验收包括三类合成文件布局、错误输入、不可覆盖保存、真实界面闭环和完整软件门禁。
通用串口文本、其他物理量和更多设备协议继续后置，避免把扩展接口等同于已验证兼容。
使用方法见 [电压文件导入](voltage-data-import.md)。

### 2026-09-09：暂停功能扩展并同步私有开发进度

TD-050 三主题、TD-051 运行与工作流改进、TD-052 电压文件导入均已完成。
最新本地验收为 3,047 个测试通过、17,567/17,567 条 package 语句覆盖，以及全新安装
CLI/GUI 验收通过；详见 [TD-052 验收报告](../reports/td-052-voltage-import-2026-09-09.md)。

所有者要求先保存暂停点和后续路线，再提交、推送现有私有开发分支并更新既有 Draft PR。
功能扩展暂停，当前以便于求职展示、解释和恢复开发为目标。
[项目恢复检查点](PROJECT_RESUME_CHECKPOINT_2026-09-09.md) 保存阶段状态和后续入口。
TD-053 暂不启动；取得真实、可解释的样本后，再选择具体输入格式或协议。

### 继续延期

**验证方式补充（2026-09-09）：** 所有者决定以本地测试为主，云端测试仅手动触发。
本地完整质量门仍适用于业务代码变动；普通 push/PR 更新不启动云端任务。
保留既有云端矩阵，只有明确需要且所有者指示时运行，详见
[本地测试与手动 CI](LOCAL_TESTING_AND_CI.md)。

- 从归档 Replay 输入一键重跑；
- READ/LIVE 原始输出 observation 持久化；
- 项目历史曲线叠加；
- manifest 签名、作者认证、多用户权限和云协作；
- Dashboard 运行时迁移与完整屏幕阅读器支持；
- 真实串口长期运行、真实 AFE 和实验室仪器验证。

这些事项并非当前产品主链缺失。只有观察到明确需求或进入对应硬件阶段后才重新排序。

## 收敛验收门

- 完整 pytest 与 package statement coverage 100%；
- Ruff、mypy、`pip check` 和 `git diff --check`；
- 从工作树外的只读源码快照隔离构建 sdist 和 wheel；
- wheel 安装到全新环境，并确认导入来自该环境的 `site-packages`；
- 普通路径和 Unicode 路径演示逐文件 SHA-256 一致；
- 安装后的 CSV_REPLAY 项目批次、manifest v3、stderr/stdout 隔离、输入归档和
  源文件改动后的历史验证；
- 隐私路径扫描和证据标签复核。

2026-09-08 历史冻结证据记录在
[功能冻结与成果收敛报告](../reports/feature-freeze-and-consolidation-2026-09-08.md)；
最新本地验收见 [TD-052 报告](../reports/td-052-voltage-import-2026-09-09.md)。

## 证据与发布边界

`SYNTHETIC`、`CSV_REPLAY`、`HOST_TEST`、`SPICE_IDEAL`、
`BENCH_CONTROLLER` 和 `BENCH` 必须始终分开。软件 PASS、确定性演示、主机性能检查
和 Replay 历史都不是硬件验证。当前已记录的最高证据仍只是既有的
`BENCH_CONTROLLER` MSP430 UART 兼容性；已验证 AFE 硬件性能仍为 0。

此前各实现检查点的“不提交、不推送、不修改 PR”约束保留为历史边界；本轮所有者已单独
授权提交和推送现有私有开发分支、更新既有 Draft PR。仍不执行 Ready for review、merge、
tag、Release、包发布、可见性修改、许可证修改或历史重写。
本次同步授权不等于完成 Software Phase 6 Step 8 或获得其他发布授权。
